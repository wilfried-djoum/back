const config = {
    db: {
        // host: "172.20.10.5",
        host: "localhost",
        user: "root",
        password: "will",
        database: "users",
        connectTimeout: 60000
    },
    listPerPage: 10,
};

module.exports = config